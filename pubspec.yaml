name: nexhub
description: "A new Flutter project."
# Private package; remove if publishing to pub.dev
publish_to: 'none'

# Versioning
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Core packages
  supabase_flutter: ^2.12.4
  flutter_riverpod: ^2.5.1

  intl: ^0.20.2
  cached_network_image: ^3.3.0
  flutter_slidable: ^4.0.3
  url_launcher: ^6.2.0
  emoji_picker_flutter: ^4.4.0
  go_router: ^17.0.0
  google_fonts: ^6.1.0
  file_picker: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
